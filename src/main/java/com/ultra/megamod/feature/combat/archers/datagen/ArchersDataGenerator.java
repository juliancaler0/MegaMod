package com.ultra.megamod.feature.combat.archers.datagen;

import com.ultra.megamod.feature.combat.archers.ArchersMod;
import com.ultra.megamod.feature.combat.archers.content.ArcherSounds;
import com.ultra.megamod.feature.combat.archers.content.ArcherSpells;
import com.ultra.megamod.feature.combat.archers.item.ArcherArmors;
import com.ultra.megamod.feature.combat.archers.item.ArcherWeapons;

/**
 * Data generation helper for the Archers system.
 * In MegaMod NeoForge context, data generation is handled via static JSON resources.
 * This class is retained for reference and documentation of the data generation patterns
 * for spells, sounds, weapon attributes, item tags, and spell tags.
 *
 * Original Fabric datagen providers:
 * - SoundGen: generates sounds.json from ArcherSounds.entries
 * - SpellGen: generates spell JSON files from ArcherSpells.entries
 * - SpellTagGenerator: generates spell tags (books, scrolls, treasure)
 * - ItemTagGenerator: generates weapon and armor item tags
 * - UnsmeltGenerator: generates smelting/blasting recipes for disassembly
 * - ArcherRecipes: generates crafting recipes
 * - WeaponGen: generates weapon attribute JSON files
 */
public class ArchersDataGenerator {
    // Intentionally empty - data generation is handled via static JSON resources
    // See ArcherRecipes for recipe documentation
}
